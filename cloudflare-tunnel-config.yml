tunnel: 26f86a83-0b6f-4ccd-969e-e2f3b2c80a24
credentials-file: C:\Users\NoobCity\.cloudflared\26f86a83-0b6f-4ccd-969e-e2f3b2c80a24.json

ingress:
  # Backend API for coffee machine
  - hostname: coffee-api.hydromods.org
    service: http://localhost:3000
    originRequest:
      httpHostHeader: localhost:3000
      # Ensure CORS headers are passed through
      connectTimeout: 30s
      tlsTimeout: 30s
      tcpKeepAlive: 30s
      keepAliveConnections: 100
      keepAliveTimeout: 90s
  
  # Frontend kiosk interface  
  - hostname: k2.hydromods.org
    service: http://localhost:3001
    originRequest:
      httpHostHeader: localhost:3001
      connectTimeout: 30s
      tlsTimeout: 30s
      tcpKeepAlive: 30s
      keepAliveConnections: 100
      keepAliveTimeout: 90s
  
  # Alternative frontend hostname
  - hostname: coffee.hydromods.org
    service: http://localhost:3001
    originRequest:
      httpHostHeader: localhost:3001
  
  # Catch-all rule (required)
  - service: http_status:404
